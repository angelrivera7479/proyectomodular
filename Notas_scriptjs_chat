obtenerRespuesta(input){
respuesta =  procesarConsulta(input)
mostrarMensaje(Respuesta)
}

procesarConsulta(consulta){
consulMin = normalizarConsulta(consulta)
switch(consulMin){
	obtenerInformacionLugar(consulMin)
	obtenerInformacionPlaya(consulMin)
	obtenerInformacionLago(consulMin)
	}
}

- Ahorita estamos en procesarConsulta
Todo lo que se necesita se declara ahi mismo, a excepcion de: 	
	obtenerInformacionLugar(consulMin)
	obtenerInformacionPlaya(consulMin)
	obtenerInformacionLago(consulMin)
	
- Pasamos a analizar obtenerInformacionLugar()	<-Parece que ya esta
	obtenerEstadoDesdeConsulta parece no tener problemas
	aplicarReglasSinonimosUbicacion parece no tener problemas
	mostrarEstado es una funcion larga, parece que aqui esta el problema

- En mostrarEstado()
Se declara un arreglo con los NOMBRES de los estados 
Para que este metodo se encarga de la ventana emergente y seleccionar el estado. Haremos pruebas con el codigo de replit
Comentamos la funcion de  mostrarEstado() al igual que las llamadas a esta en el codigo

- Pasamos a analizar obtenerInformacionPlaya(), contiene:
	buscarEstado() parece no tener problemas
	obtenerEstadoDesdeConsulta() parece no tener problemas
	aplicarReglasSinonimosClima() parece no tener problemas
	aplicarReglasSinonimosUbicacion() parece no tener problemas
	aplicarReglasTemperatura() parece no tener problemas
	
- Pasamos a analizar obtenerInformacionLago()
	
